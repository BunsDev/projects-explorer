# ============================================================
# Projects Explorer - Environment Variables
# ============================================================
# Copy this file to .env.local and fill in your values
# 
# NEVER commit .env.local to version control!
# ============================================================

# ------------------------------------------------------------
# DATABASE_URL - Neon PostgreSQL Connection String
# ------------------------------------------------------------
# Where to get it:
#   1. Go to https://console.neon.tech
#   2. Create a new project (or select existing)
#   3. Click "Connection Details" in the sidebar
#   4. Copy the "Connection string"
#
# Format: postgresql://[user]:[password]@[host]/[database]?sslmode=require
#
# Tips:
#   - Use "Pooled connection" for production (handles more concurrent connections)
#   - Free tier: 0.5 GB storage, auto-suspend after 5 minutes of inactivity
#   - The connection string should end with ?sslmode=require
#
DATABASE_URL="postgresql://user:password@ep-example-123456.us-east-2.aws.neon.tech/neondb?sslmode=require"

# ------------------------------------------------------------
# ADMIN_PASSWORD - Dashboard Login Password
# ------------------------------------------------------------
# Where to get it:
#   Choose your own secure password!
#
# Tips:
#   - Use at least 12 characters
#   - Include uppercase, lowercase, numbers, and symbols
#   - Generate a secure password: openssl rand -base64 32
#   - This is what you'll use to login at /login
#
# WARNING: Don't use simple passwords in production!
#
ADMIN_PASSWORD="your-secure-password-here"

# ------------------------------------------------------------
# BLOB_READ_WRITE_TOKEN - Vercel Blob Storage Token
# ------------------------------------------------------------
# Where to get it:
#   1. Go to https://vercel.com/dashboard
#   2. Select your project (deploy first if needed)
#   3. Go to "Storage" tab
#   4. Click "Create Database" → Select "Blob"
#   5. After creation, copy BLOB_READ_WRITE_TOKEN from environment variables
#
# Alternative (for local development):
#   1. Go to https://vercel.com/dashboard/stores
#   2. Click "Create" → "Blob"
#   3. Copy the token from the "Environment Variables" section
#
# Tips:
#   - This token allows read/write access to your blob storage
#   - Each Vercel project can have its own Blob store
#   - Free tier: 5 GB storage, 1 GB egress/month
#
BLOB_READ_WRITE_TOKEN="vercel_blob_rw_xxxxxxxxxxxxxxxxxxxx"

# ============================================================
# Optional Variables (not currently used but may be helpful)
# ============================================================

# NODE_ENV - Set by Next.js automatically
# NODE_ENV="development"

# NEXT_PUBLIC_* - Public environment variables (exposed to browser)
# Use this prefix for any values you need client-side
# NEXT_PUBLIC_APP_URL="http://localhost:3000"

# ------------------------------------------------------------
# ALLOWED_IPS - IP Allowlist for Admin Access (optional)
# ------------------------------------------------------------
# Comma-separated list of allowed IPs or CIDR ranges
# Leave empty to allow all IPs
#
# Examples:
#   - Single IP: "203.0.113.50"
#   - Multiple IPs: "203.0.113.50,198.51.100.25"
#   - CIDR range: "192.168.1.0/24"
#   - Mixed: "10.0.0.1,192.168.0.0/16"
#
ALLOWED_IPS=""

# ------------------------------------------------------------
# EMERGENCY_BYPASS_TOKEN - Lockout Recovery (optional)
# ------------------------------------------------------------
# Use this token to bypass IP restrictions and rate limiting
# when you get locked out of your account.
#
# Generate a secure token:
#   openssl rand -base64 32
#
# IMPORTANT:
#   - Keep this token secret and store it safely offline
#   - You still need the correct ADMIN_PASSWORD to login
#   - The bypass token only skips IP and rate limit checks
#
EMERGENCY_BYPASS_TOKEN=""

# ------------------------------------------------------------
# Rate Limiting Settings (optional)
# ------------------------------------------------------------
# Control how aggressive the rate limiting is
#
# RATE_LIMIT_MAX_ATTEMPTS: Number of failed attempts before blocking
#   Default: 5
#
# RATE_LIMIT_WINDOW_MINUTES: Time window for counting failed attempts
#   Default: 15
#
# Example: 5 failed attempts in 15 minutes = temporarily blocked
#
RATE_LIMIT_MAX_ATTEMPTS="5"
RATE_LIMIT_WINDOW_MINUTES="15"